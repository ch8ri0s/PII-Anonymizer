/**
 * PDF Table Detection Type Definitions (Electron Re-export)
 *
 * Re-exports all types from the shared module.
 */

export type {
  PdfTextItem,
  BoundingBox,
  Alignment,
  DetectionMethod,
  TableCell,
  TableRow,
  TableStructure,
  PdfTableDetectionResult as DetectionResult,
} from '../../shared/dist/types/index.js';
