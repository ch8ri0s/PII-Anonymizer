
  # File PII Sanitization App

  This is a code bundle for File PII Sanitization App. The original project is available at https://www.figma.com/design/ZSC8bnD3MVud6CKNm5Hgho/File-PII-Sanitization-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  